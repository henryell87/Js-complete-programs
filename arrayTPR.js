numarray(5);
numarray(12);

// This program loads the array and sets the number in the array

function numarray(n) {
    resultArr = [n];
    for (i = 0; i < n; i++) {
        resultArr[i] = i ;
    }
    console.log(resultArr);
}

//This starts my method / app


numarray(5);
numarray(12);